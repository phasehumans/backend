// npm node package manager

/* 
- node package manager
- get external packages locally
- external dependency / library / package / module is reusable code that others has written
    - internal packages: fs, http, path
    - external packages: nodemailer, express, jwt

- npm install : install all dependcies from package.json dependecnies section
- cli: command line interface; npm, ls, pwd


*/

const fs= require('fs')
// to use fs we dont npm install fs; it is internal package which comes w/ nodejs
